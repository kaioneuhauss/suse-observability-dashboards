nodes:
- _type: Monitor
  name: SRE · etcd apply backlog above 1000
  identifier: urn:custom:monitor:sre-dashboards:etcd-lag
  description: Checks etcd apply backlog above 1000. Values are evaluated every 60
    seconds. Windows apply to observed samples; inspect collection health before interpreting
    a missing result. Thresholds are starting points to review against the workload
    baseline.
  arguments:
    metric:
      query: min_over_time((clamp_min(max by (cluster_name,node) (etcd_server_proposals_committed_total{job="sre-etcd"})
        - max by (cluster_name,node) (etcd_server_proposals_applied_total{job="sre-etcd"}),0))[5m:30s])
      unit: none
      aliasTemplate: ${cluster_name} · ${node}
    comparator: GT
    threshold: 1000
    failureState: DEVIATING
    urnTemplate: urn:kubernetes:/${cluster_name}:node/${node}
    titleTemplate: SRE · etcd apply backlog above 1000 · ${cluster_name} · ${node}
  intervalSeconds: 60
  status: ENABLED
  remediationHint: '1. Identify the cluster and node in the title. Check etcd quorum
    and peer connectivity.

    2. Compare leader changes, WAL writes, database commits, peer round trips, pod
    restarts and previous logs at the same time.

    3. Preserve snapshots before recovery. Never remove a member or restore a snapshot
    based on this alert alone.'
  tags:
  - sre-dashboards
  - dashboard-platform
  function: {{ get "urn:stackpack:common:monitor-function:threshold" }}
